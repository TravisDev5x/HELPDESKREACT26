APP_NAME=HelpdeskReact
APP_ENV=local
APP_KEY=base64:k0qgsRsv62XAuIEEuCiAsPQknzEDSnDi7ddjKJ/qDR4=
APP_DEBUG=true
APP_URL=http://127.0.0.1:8001

APP_LOCALE=es
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=es_MX

APP_MAINTENANCE_DRIVER=file

# =====================================================
# HASHING (ARGON2ID - ACTIVO)
# =====================================================
HASH_DRIVER=argon2id
BCRYPT_ROUNDS=12
ARGON_MEMORY=65536
ARGON_THREADS=1
ARGON_TIME=4
HASH_VERIFY=true

# =====================================================
# LOGS
# =====================================================
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# =====================================================
# BASE DE DATOS
# =====================================================
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=helpdeskdata_smoke
DB_USERNAME=root
DB_PASSWORD=

# =====================================================
# SESIONES (CRÍTICO PARA SANCTUM)
# =====================================================
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=127.0.0.1
SESSION_SECURE_COOKIE=false
SESSION_SAME_SITE=lax

# =====================================================
# SANCTUM (SPA FIRST-PARTY)
# =====================================================
SANCTUM_STATEFUL_DOMAINS=127.0.0.1,localhost

# =====================================================
# FILESYSTEM
# =====================================================
FILESYSTEM_DISK=public

# =====================================================
# CACHE / QUEUES (LOCAL SEGURO)
# =====================================================
CACHE_STORE=file
QUEUE_CONNECTION=sync

# =====================================================
# REDIS (NO USADO EN LOCAL)
# =====================================================
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# =====================================================
# MAIL (LOG)
# =====================================================
MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# =====================================================
# AWS (NO USADO)
# =====================================================
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# =====================================================
# FRONTEND / VITE
# =====================================================
VITE_APP_NAME="${APP_NAME}"
VITE_APP_URL=http://127.0.0.1:8001

SECURITY_HEADERS_ENABLED=true

