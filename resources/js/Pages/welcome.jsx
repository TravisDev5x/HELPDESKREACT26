import { Card, CardContent } from "@/components/ui/card";

export default function Welcome() {
    return (
        <Card className="border-0 shadow-none bg-transparent">
            <CardContent className="p-10 text-xl font-bold">
                Laravel + React + Inertia ⚙️🚀
            </CardContent>
        </Card>
    );
}
